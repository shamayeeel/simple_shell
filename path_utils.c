#include "shell.h"

/**
 * is_cmd - Determines if a file is an executable command.
 * @info: The info struct.
 * @path: Path to the file.
 *
 * Return: 1 if true, 0 otherwise.
 */
int is_cmd(info_t *info, char *path)
{
	/* Determines if a file is an executable command */
}

/**
 * dup_chars - Duplicates characters from the PATH string.
 * @pathstr: The PATH string.
 * @start: Starting index.
 * @stop: Stopping index.
 *
 * Return: Pointer to the new buffer.
 */
char *dup_chars(char *pathstr, int start, int stop)
{
	/* Duplicates characters from the PATH string */
}

/**
 * find_path - Finds the command in the PATH string.
 * @info: The info struct.
 * @pathstr: The PATH string.
 * @cmd: The command to find.
 *
 * Return: Full path of cmd if found or NULL.
 */
char *find_path(info_t *info, char *pathstr, char *cmd)
{
	/* Finds the command in the PATH string */
}
#include "shell.h"

/**
 *  * is_cmd - Determines if a file is an executable command.
 *   * @info: The info struct.
 *    * @path: Path to the file.
 *     *
 *      * Return: 1 if true, 0 otherwise.
 *       */
int is_cmd(info_t *info, char *path)
{
		/* Determines if a file is an executable command */
}

/**
 *  * dup_chars - Duplicates characters from the PATH string.
 *   * @pathstr: The PATH string.
 *    * @start: Starting index.
 *     * @stop: Stopping index.
 *      *
 *       * Return: Pointer to the new buffer.
 *        */
char *dup_chars(char *pathstr, int start, int stop)
{
		/* Duplicates characters from the PATH string */
}

/**
 *  * find_path - Finds the command in the PATH string.
 *   * @info: The info struct.
 *    * @pathstr: The PATH string.
 *     * @cmd: The command to find.
 *      *
 *       * Return: Full path of cmd if found or NULL.
 *        */
char *find_path(info_t *info, char *pathstr, char *cmd)
{
		/* Finds the command in the PATH string */
}

